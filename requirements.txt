# AI Analyst Jobs — Python Dependencies
# Requires Python >= 3.10 (use: uv venv --python 3.11 .venv)
# Core pipeline (job_db_pipeline.py) uses only stdlib.
# LinkedIn collection (collect_jobs.py) requires these:
python-jobspy>=1.1.0
pandas==2.3.2

# Optional: dashboard generation
plotly==6.3.0

# Dev dependencies
pytest>=7.0
ruff>=0.4.0
